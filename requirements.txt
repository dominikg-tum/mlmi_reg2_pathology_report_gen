# Legacy pip install — prefer: uv pip install -e ".[dev]" (see pyproject.toml)
numpy>=1.24
pyyaml>=6.0
openai>=1.0
pytest>=7.0
scikit-learn>=1.3
pillow>=10.0

# Frontend: streamlit run app.py
# streamlit>=1.32

# Cluster GPU jobs: uv pip install -e ".[dev,cluster]"
# torch
# transformers
# openslide-python
# huggingface_hub
# tqdm

# Optional eval / training
# pandas
# rouge-score
# sacrebleu
# sentence-transformers

# UNI2 WSI embedding jobs
# torch
# torchvision
# timm
