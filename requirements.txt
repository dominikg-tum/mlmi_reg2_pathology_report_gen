# Legacy pip install — prefer: uv pip install -e ".[dev]" (see pyproject.toml)
numpy>=1.24
pyyaml>=6.0
openai>=1.0
pytest>=7.0
scikit-learn>=1.3
pillow>=10.0

# Frontend: streamlit run app.py
# streamlit>=1.32

# Cluster GPU jobs: uv pip install -e ".[dev,cluster]"
# torch
# transformers
# openslide-python
# huggingface_hub
# tqdm

# RAG (HybridRAG + HippoRAG fallback): uv pip install -e ".[dev,rag]"
langchain-core>=0.3
langchain-community>=0.3
langchain-huggingface>=0.1
langchain-chroma>=0.2
langchain-classic>=0.3
rank-bm25>=0.2
sentence-transformers>=3.0
pandas>=2.0
openpyxl>=3.1

# Optional eval / training
# pandas
# rouge-score
# sacrebleu
# sentence-transformers
