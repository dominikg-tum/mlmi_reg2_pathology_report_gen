# Legacy pip install — prefer: uv pip install -e ".[dev]" (see pyproject.toml)
numpy>=1.24
pyyaml>=6.0
openai>=1.0
pytest>=7.0
scikit-learn>=1.3
pillow>=10.0

# Frontend: streamlit run app.py
# streamlit>=1.32

# Cluster GPU jobs: uv pip install -e ".[dev,cluster]"
# torch
# transformers
# openslide-python
# huggingface_hub
# tqdm

# RAG System
# langchain
# langchain-community
# langchain-huggingface
# langchain-chroma
# rank-bm25
# sentence-transformers

# Optional eval / training
# pandas
# rouge-score
# sacrebleu
# sentence-transformers

# LoRA fine-tuning (cluster GPU only — see scripts/cluster/train_lora.sh)
# Qwen3-VL needs a RECENT transformers; do NOT pin to the TITAN 4.46.0 used for data build.
# torch
# transformers>=4.57
# peft
# accelerate
# safetensors>=0.4
