# Graph data

| File | Owner | Purpose |
|------|-------|---------|
| `execution_graph.jsonl` | DOGA | Agent traversal graph (one JSON object per line) |
| `ontology_graph.jsonl` | DOGA | Optional full drawio taxonomy mirror |

Schema: see `docs/PROJECT_OVERVIEW.md` §4.
