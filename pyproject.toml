[project]
name = "mlmi-reg2-pathology-report-gen"
version = "0.1.0"
description = "Agentic pathology report generation from uterine WSIs"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.24",
    "pyyaml>=6.0",
    "scikit-learn>=1.3",
    "pillow>=10.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "openai>=1.0",
]
# Install on cluster inside dominik_20260529_base (or via enroot) for GPU jobs
cluster = [
    "torch>=2.0",
    "transformers>=4.40",
    "openslide-python>=1.3",
    "tqdm>=4.66",
    "huggingface_hub>=0.23",
    "einops>=0.7",
    "einops-exts>=0.0.4",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=7.0",
    "openai>=1.0",
]

[tool.hatch.build.targets.wheel]
packages = [
    "agent",
    "baselines",
    "eval",
    "extraction",
    "graph",
    "memory",
    "retrieval",
    "training",
    "vision",
]
